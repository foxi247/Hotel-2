# 🏨 Халачи Гостиница — Полный веб-проект

**Версия:** 1.0.0  
**Дата:** 17 февраля 2026

---

## 📋 Описание

Полноценный веб-сайт для гостиницы "Халачи" в Дербенте с админ-панелью и Node.js бэкендом.

### Функционал

**Публичная часть (index.html):**
- Главная страница с бронированием
- Каталог номеров с ценами
- Секция туров и экскурсий
- Карта расположения
- Отзывы гостей
- Форма заявки на бронирование
- Адаптивный дизайн

**Админ-панель (admin.html):**
- Дашборд со статистикой
- Управление номерами
- CRUD для туров
- Управление категориями
- Просмотр заявок
- Настройки сайта

---

## 🚀 Установка и запуск

### Требования
- Node.js 16+ 
- npm или yarn

### Установка

```bash
# 1. Перейдите в папку проекта
cd halachi-hotel

# 2. Установите зависимости
npm install

# 3. Запустите сервер
npm start
```

### Доступ

- **Сайт:** http://localhost:3000
- **Админ-панель:** http://localhost:3000/admin

**Пароль администратора:** `halachi2024`

---

## 📁 Структура проекта

```
halachi-hotel/
├── server.js              # Node.js сервер (Express)
├── package.json           # Зависимости
├── data/
│   └── database.json     # База данных (JSON)
├── public/
│   ├── index.html       # Главная страница
│   ├── admin.html       # Админ-панель
│   ├── css/
│   │   ├── style.css    # Стили сайта
│   │   └── admin.css    # Стили админки
│   └── js/
│       ├── app.js        # Логика сайта
│       └── admin.js      # Логика админки
└── README.md             # Этот файл
```

---

## 🔧 API Endpoints

### Публичные
| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/api/data` | Все данные |
| GET | `/api/hotel` | Информация о гостинице |
| GET | `/api/tours` | Список туров |
| GET | `/api/rooms` | Список номеров |
| POST | `/api/booking` | Отправить заявку |

### Админские (требуется пароль)
| Метод | Endpoint | Описание |
|-------|----------|----------|
| POST | `/api/admin/hotel` | Обновить инфо о гостинице |
| POST | `/api/admin/tours` | Добавить/обновить тур |
| DELETE | `/api/admin/tours/:id` | Удалить тур |
| POST | `/api/admin/categories` | Управление категориями |
| POST | `/api/admin/visitor-count` | Обновить счётчик |
| GET | `/api/admin/bookings` | Список заявок |

**Заголовок:** `x-admin-password: halachi2024`

---

## 🎨 Технологии

- **Frontend:** HTML5, CSS3, Vanilla JavaScript
- **Backend:** Node.js, Express.js
- **Данные:** JSON файлы
- **Дизайн:** Modern CSS с Flexbox/Grid
- **Адаптивность:** Mobile-first подход

---

## 📝 Настройка

### Изменение данных

Редактируйте файл `data/database.json` для изменения:
- Информации о гостинице
- Номеров и цен
- Туров и экскурсий
- Категорий

### Изменение пароля

В файле `server.js` найдите строку:
```javascript
const adminPassword = 'halachi2024';
```

---

## 🛠️ Разработка

```bash
# Режим разработки с автоперезагрузкой
npm install -g nodemon
nodemon server.js
```

---

## 📦 Развёртывание

### На VDS/Сервере
1. Загрузите файлы на сервер
2. Выполните `npm install`
3. Настройте PM2: `pm2 start server.js --name halachi`
4. Настройте Nginx как прокси

### На хостинге (без Node.js)
Для статического хостинга используйте только папку `public/` + JSON файлы в `data/`.

---

## 📄 Лицензия

MIT License

---

**Создано:** Джарвис (Jarvis AI Assistant)  
**Дата:** 17 февраля 2026
